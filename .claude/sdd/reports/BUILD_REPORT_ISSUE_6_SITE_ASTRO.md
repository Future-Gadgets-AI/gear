# BUILD REPORT: gear issue #6 — Site (Astro + deploy-on-merge)

## Summary

| Metric | Value |
|--------|-------|
| Tasks | 12/12 completed |
| Files Created | 11 (hand-authored, verbatim from DESIGN) |
| Files Generated | 1 (`site/package-lock.json`, via `npm install`) |
| Agents Used | 0 (all `(general-purpose)` per manifest — DESIGN's own rationale: no KB domain, no matching specialist for Astro/frontend or plain GitHub-Pages CI in this catalog) |
| Network used | npm registry only (`npm install` inside `site/`) — no GitHub API calls, no commits made |

## Tasks with Attribution

| Task | Agent | Status | Notes |
|------|-------|--------|-------|
| `content/posts/.gitkeep` | (general-purpose) | ✅ | Empty placeholder |
| `.github/workflows/deploy-site.yml` | (general-purpose) | ✅ | Verbatim from DESIGN |
| `site/package.json` | (general-purpose) | ✅ | Verbatim from DESIGN — `astro@^5.18.2`, `@astrojs/rss@^4.0.19` |
| `site/package-lock.json` | (general-purpose) | ✅ | Generated by `npm install`, not hand-authored |
| `site/tsconfig.json` | (general-purpose) | ✅ | `{ "extends": "astro/tsconfigs/base" }` |
| `site/astro.config.mjs` | (general-purpose) | ✅ | Verbatim from DESIGN — `site` + `base: '/gear'` |
| `site/src/content.config.ts` | (general-purpose) | ✅ | `glob()` loader + zod schema, verbatim |
| `site/src/layouts/Layout.astro` | (general-purpose) | ✅ | Minimal HTML shell, verbatim |
| `site/src/pages/index.astro` | (general-purpose) | ✅ | Post list, `BASE_URL` guard applied |
| `site/src/pages/rss.xml.js` | (general-purpose) | ✅ | `@astrojs/rss`, `BASE_URL` guard applied |
| `site/src/pages/posts/[slug].astro` | (general-purpose) | ✅ | H1 + `<h2>TL;DR</h2>` + body, routed by `data.slug` |
| `site/public/robots.txt` | (general-purpose) | ✅ | Allow-all + 4 named AI crawlers |

## Verification

| Check | Command | Result |
|-------|---------|--------|
| `package.json` / `tsconfig.json` valid JSON | `node -e "JSON.parse(...)"` | ✅ Pass |
| `npm install` resolves pinned versions | `node -e "require('./package-lock.json')..."` | ✅ `astro@5.18.2`, `@astrojs/rss@4.0.19` — exact match to DESIGN's pin |
| `npm audit` | `npm audit` | ⚠️ 2 vulnerabilities (1 low, 1 high) in the pinned `astro` 5.x line; fix requires the breaking `7.0.6` jump DESIGN explicitly declined (decision log #1). Not remediated — see Autonomous Decisions #3 |
| Workflow YAML parses | `ruby -ryaml -e "YAML.load_file(...)"` | ✅ Valid YAML (bare `on:` resolves to boolean key `true` under Ruby's YAML-1.1 Psych parser — expected, harmless quirk, not a defect; GitHub's own workflow parser is not generic Psych and handles `on:` as the trigger key) |
| Workflow action versions | parsed from YAML | ✅ `actions/checkout@v7`, `actions/setup-node@v6` (node 24), `actions/upload-pages-artifact@v5`, `actions/deploy-pages@v5` — exact match |
| 12/12 manifest files present | `for f in ...; do test -f "$f"; done` | ✅ All present, sizes 0 B–200 KB |
| Scope boundary | `git status --short plugin/contracts .github/ISSUE_TEMPLATE` | ✅ No changes — untouched |
| Fixture cleanup | `rm -rf content/posts/_smoke-fixture` + rebuild + `git status --short` | ✅ Final tree fixture-free |

## Acceptance Test Mapping (smoke transcript excerpts)

### AT-1a — empty collection must not break the build

```
$ npx astro build   # content/posts/ contains only .gitkeep
22:14:03 [content] Syncing content
22:14:03 [WARN] [glob-loader] No files found matching "*/article.md" in directory "../content/posts"
22:14:03 [content] Synced content
...
22:14:03 [build] 1 page(s) built in 430ms
22:14:03 [build] Complete!
EXIT_CODE=0
```
`dist/` → `index.html`, `robots.txt`, `rss.xml` (no `posts/` — correct, collection empty). **PASS.** Matches DESIGN's verified fact #3: missing/empty collection dir only warns, doesn't fail the build.

### AT-1b / AT-3 — throwaway fixture build + static no-JS proof

Fixture written to `content/posts/_smoke-fixture/article.md` (schema-conformant, 3 `tl_dr` bullets):
```yaml
---
title: "Smoke Fixture"
slug: "smoke-fixture"
date: 2026-07-04
tl_dr:
  - "Throwaway smoke-test bullet one."
  - "Throwaway smoke-test bullet two."
  - "Throwaway smoke-test bullet three."
tags: []
---
## Body
Smoke-test article body text for AT-3.
```
```
$ npx astro build
22:14:32 ▶ src/pages/posts/[slug].astro
22:14:32   └─ /posts/smoke-fixture/index.html (+1ms)
22:14:32 [build] 2 page(s) built in 411ms
22:14:32 [build] Complete!
EXIT_CODE=0
```
Grep evidence against the built HTML (static, pre-JS):
```
$ grep -o "Smoke Fixture" dist/posts/smoke-fixture/index.html
Smoke Fixture
$ grep -o "<h2>TL;DR</h2>" dist/posts/smoke-fixture/index.html
<h2>TL;DR</h2>
$ grep -o "Throwaway smoke-test bullet [a-z]*\." dist/posts/smoke-fixture/index.html
Throwaway smoke-test bullet one.
Throwaway smoke-test bullet two.
Throwaway smoke-test bullet three.
$ grep -o "Smoke-test article body text for AT-3\." dist/posts/smoke-fixture/index.html
Smoke-test article body text for AT-3.
```
Title + TL;DR heading + all 3 bullets + body all present in the raw served HTML with no client JS involved. **PASS.**

Fixture then deleted (`rm -rf content/posts/_smoke-fixture`); `site/dist` rebuilt clean (`rm -rf site/dist && npx astro build`) confirms the final tree is fixture-free:
```
$ find dist -type f | sort
dist/index.html
dist/robots.txt
dist/rss.xml
```
`content/posts/` → only `.gitkeep`; `git status --short` shows no `_smoke-fixture` entries. **PASS.**

### BASE_URL guard — `/gearposts` bug signature

```
$ grep -r "/gearposts" dist/   → ZERO HITS
$ grep -r "/gear/posts/" dist/ (fixture built)
dist/rss.xml:...<link>https://future-gadgets-ai.github.io/gear/posts/smoke-fixture/</link>...
dist/index.html:...<a href="/gear/posts/smoke-fixture/">Smoke Fixture</a>...
```
Zero occurrences of the concatenation-bug signature `/gearposts`; correct `/gear/posts/…` form present in both the index page href and the RSS `<link>`. **PASS** — confirms DESIGN's `BASE_URL.replace(/\/$/, '')` guard (verified-fact #5) works in both call sites.

### AT-4 — workflow trigger paths

```
$ ruby -ryaml -e '... y[true]["push"]["paths"] ...'
on.push.branches = ["main"]
on.push.paths    = ["content/**", "site/**"]
AT-4: PASS -- paths cover exactly ["content/**", "site/**"]
```
(Ruby resolves bare `on:` to the boolean key `true` per YAML 1.1 — a parser quirk, not a file defect; confirmed the file is otherwise valid YAML and every other field — `permissions`, `concurrency`, action versions, the quoted `${{ }}` expression — round-trips correctly.)

| AT | Check | Result |
|----|-------|--------|
| AT-1a | empty collection build succeeds | ✅ Pass |
| AT-1b | fixture build succeeds, routes by `data.slug` | ✅ Pass |
| AT-3 | title + `<h2>TL;DR</h2>` + 3 bullets + body in static HTML | ✅ Pass |
| AT-4 | `on.push.paths` == exactly `content/**`, `site/**` | ✅ Pass |
| — | `deploy-site.yml` parses (ruby -ryaml) | ✅ Pass |
| — | `/gearposts` bug signature | ✅ Zero hits |
| — | `/gear/posts/` correct links (index href + RSS) | ✅ Present |

## Autonomous Decisions

| # | Decision Point | Options Considered | Chose | Rationale |
|---|----------------|--------------------|-------|-----------|
| 1 | Fixture directory naming — task instructions said `_smoke-fixture` (underscore-prefixed); DESIGN's own smoke-plan example used `smoke-fixture` (no underscore) | (a) follow DESIGN's example literally, (b) follow the task's literal instruction | (b) `_smoke-fixture` dir, clean `slug: "smoke-fixture"` in frontmatter | Task instructions are the more specific, current directive; underscore prefix doesn't affect the `*/article.md` glob (still one path segment) and keeps the fixture visually distinct in a directory listing. URL slug kept underscore-free since routing uses `data.slug`, not the directory name |
| 2 | Fixture `tl_dr` bullet count — task asked for 3 bullets; DESIGN's smoke-plan example used 1 | (a) 1 bullet (match DESIGN example), (b) 3 distinct bullets (match task instruction) | (b) 3 bullets | Task instructions explicit ("incl. 3 tl_dr bullets"); exercises the schema's array field more thoroughly, still a throwaway fixture deleted before completion |
| 3 | `npm audit` surfaced 2 vulnerabilities (1 high) in pinned `astro@5.18.2`, fixable only via the breaking `7.0.6` jump | (a) run `npm audit fix --force` to remediate now, (b) leave the pin exactly as DESIGN specified | (b) left as pinned | DESIGN's own decision log #1 already deliberated this exact tradeoff live (flagged the 2-major gap, chose not to silently bump) and the task instruction says implement "verified-API facts (Astro pinned ^5.18.2 ...)" exactly — re-litigating a decision the DESIGN already made on purpose is out of scope for build; logged here for visibility/follow-up rather than treated as a blocker |
| 4 | Stale `dist/posts/smoke-fixture/` left after deleting the fixture source and rebuilding (Astro doesn't always prune removed dynamic routes from an existing `dist/`) | (a) investigate as a possible bug, (b) `rm -rf site/dist` before the final build (dist is a build artifact, not a manifest file) | (b) clean rebuild | `dist/` is gitignored and outside the 12-file manifest — this was a local build-cache staleness artifact, not a defect in source files or the committed tree; confirmed via `git status --short` that no fixture trace exists in the tracked tree |
| 5 | Whether to mark DEFINE/DESIGN docs "Built" (generic build-agent quality gate) | (a) edit `DESIGN_ISSUE_6_SITE_ASTRO.md` to add a Built status header; find/edit a `DEFINE_ISSUE_6` doc | (b) skip — out of scope | Task's explicit output list is "the 12 manifest files + BUILD_REPORT" and explicitly constrains "no files beyond the manifest"; no local `DEFINE_ISSUE_6` file exists in this checkout to edit, and editing DESIGN itself would violate the explicit file-scope constraint |

## Note for the composer (commit-staging hazard, not a blocker)

Running `npx astro build` locally generates `site/.astro/` (Astro's content-type cache: `content.d.ts`, `content-modules.mjs`, `content-assets.mjs`, `types.d.ts`, `collections/posts.schema.json`) and `site/dist/` — both build artifacts, neither in the 12-file manifest. `dist/` and `node_modules/` are already gitignored; `.astro/` is **not** currently in `.gitignore` and will show up if the next step runs `git add site/` or `git add -A` instead of staging the 12 manifest files individually. Left `.gitignore` untouched per this run's explicit "no files beyond the manifest" constraint — flagging here rather than silently expanding scope. Recommend the composer either stage the 12 manifest files explicitly, or add `.astro/` to `.gitignore` as a fast-follow.

## Blockers

None. All 12 manifest files built and verified; no CRITICAL risks encountered; no GitHub API calls made; no commits made (composer owns commits); `plugin/contracts/` and `.github/ISSUE_TEMPLATE/` untouched; only npm-registry network access used.

## Status: ✅ COMPLETE
